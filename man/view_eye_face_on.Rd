% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viewing.R
\name{view_eye_face_on}
\alias{view_eye_face_on}
\title{Plot a Compound Eye in a Standardised Face-On View}
\usage{
view_eye_face_on(
  df,
  coord_x = "x",
  coord_y = "y",
  coord_z = "z",
  normal_x = "norm.x",
  normal_y = "norm.y",
  normal_z = "norm.z",
  projection = c("2D", "3D"),
  view = NULL,
  view_direction = NULL,
  up = c(0, 1, 0),
  reverse = FALSE,
  long_axis_vertical = TRUE,
  add = FALSE,
  translate_x = 0,
  translate_y = 0,
  col = "black",
  pch = ".",
  cex = 1,
  rgl_size = 2,
  fov = 0,
  zoom = 1,
  axes = TRUE,
  xlab = "x",
  ylab = "y"
)
}
\arguments{
\item{df}{A data frame or tibble containing three-dimensional point
coordinates and, unless \code{view} or \code{view_direction} is supplied,
surface-normal vectors.}

\item{coord_x}{Character. Name of the x-coordinate column. Default: \code{"x"}.}

\item{coord_y}{Character. Name of the y-coordinate column. Default: \code{"y"}.}

\item{coord_z}{Character. Name of the z-coordinate column. Default: \code{"z"}.}

\item{normal_x}{Character. Name of the x-component of the surface normals.
Default: \code{"norm.x"}.}

\item{normal_y}{Character. Name of the y-component of the surface normals.
Default: \code{"norm.y"}.}

\item{normal_z}{Character. Name of the z-component of the surface normals.
Default: \code{"norm.z"}.}

\item{projection}{Character. Plotting mode. \code{"2D"} produces a face-on
orthographic projection using base R. \code{"3D"} displays the centred cloud
using \code{rgl} and applies the equivalent face-on viewpoint. Default:
\code{c("2D", "3D")}, i.e. \code{"2D"}.}

\item{view}{Optional view object previously returned by
\code{view_eye_face_on()}. Supplying it reuses the same viewing direction,
in-plane orientation, and reference dimensions. Default: \code{NULL}.}

\item{view_direction}{Optional numeric vector of length three specifying the
outward viewing direction. If \code{NULL}, it is estimated from the mean of the
valid unit surface normals. Ignored when \code{view} is supplied.}

\item{up}{Numeric vector of length three giving the preferred vertical
direction before automatic in-plane orientation. Default: \code{c(0, 1, 0)}.}

\item{reverse}{Logical. If \code{TRUE}, reverse the inferred viewing direction.
Ignored when \code{view} is supplied. Default: \code{FALSE}.}

\item{long_axis_vertical}{Logical. If \code{TRUE}, rotate the face-on view by
90 degrees when necessary so that the greater projected coordinate range
of the reference cloud lies along the displayed y-axis. No PCA is used.
Ignored when \code{view} is supplied. Default: \code{TRUE}.}

\item{add}{Logical. If \code{FALSE}, create a new plot or \code{rgl} scene. If \code{TRUE},
add a new comparison cloud to the most recent CV3D face-on plot of the
same projection type. Default: \code{FALSE}.}

\item{translate_x}{Numeric. Horizontal display translation in units of the
face-on width of the first/reference cloud. Thus \code{translate_x = 1} moves
the current cloud by exactly one reference-cloud width. Default: \code{0}.}

\item{translate_y}{Numeric. Vertical display translation in units of the
face-on height of the first/reference cloud. Thus \code{translate_y = 1} moves
the current cloud by exactly one reference-cloud height. Default: \code{0}.}

\item{col}{Point colour or vector of point colours. Default: \code{"black"}.}

\item{pch}{Plotting symbol for the two-dimensional projection. The default
\code{"."} is suitable for dense surface point clouds.}

\item{cex}{Point-size multiplier for the two-dimensional projection.
Default: \code{1}.}

\item{rgl_size}{Point size for three-dimensional \code{rgl} plotting.
Default: \code{2}.}

\item{fov}{Numeric field-of-view angle for the \code{rgl} view. \code{0} gives an
orthographic projection. Default: \code{0}.}

\item{zoom}{Numeric zoom factor for the \code{rgl} view. Default: \code{1}.}

\item{axes}{Logical. Whether axes should be drawn for a new plot.
Default: \code{TRUE}.}

\item{xlab}{Character. x-axis label for a new 2D plot. Default: \code{"x"}.}

\item{ylab}{Character. y-axis label for a new 2D plot. Default: \code{"y"}.}
}
\value{
Invisibly returns a list containing:
\itemize{
\item \code{view_direction}: unit viewing direction in the original coordinate
system;
\item \code{x_direction}: horizontal screen direction in the original coordinate
system;
\item \code{y_direction}: vertical screen direction in the original coordinate
system;
\item \code{reference_width}: face-on width of the first/reference cloud;
\item \code{reference_height}: face-on height of the first/reference cloud;
\item \code{userMatrix}: 4 by 4 matrix used for the equivalent \code{rgl} view;
\item \code{rotated_90}: whether the automatic 90-degree in-plane rotation was
applied;
\item \code{normal_resultant_length}: length of the mean unit-normal vector;
\item \code{cloud_centre}: arithmetic coordinate centroid of the current cloud;
\item \code{translate_x} and \code{translate_y}: applied dimensionless display offsets;
\item \code{translation_3d}: corresponding three-dimensional display translation;
and
\item \code{projected_coordinates}: two-dimensional coordinates actually plotted.
}

The input data are not modified.
}
\description{
Displays a compound-eye point cloud approximately face-on without modifying
the input data. The viewing direction is estimated from the mean surface-
normal direction. The eye can be displayed either as a two-dimensional
base-R projection or as an interactively rotatable three-dimensional \code{rgl}
scene.
}
\details{
Each cloud is centred independently for display. Optional translations are
therefore deliberate plotting offsets rather than translations inherited
from the input coordinate system.

Each valid surface normal is first normalised to unit length so that vector
magnitude does not influence the estimated viewing direction. Their mean
direction is then normalised and used as the viewing axis.

The preferred \code{up} vector is projected onto the plane perpendicular to the
viewing direction. Together with the viewing direction, this defines the
face-on coordinate system. If \code{long_axis_vertical = TRUE}, only a possible
90-degree in-plane rotation is added so that the greater projected range of
the reference eye runs vertically. No principal-component analysis is
performed.

Before plotting, every supplied cloud is centred on its own arithmetic
coordinate centroid. Any original translation between separately supplied
clouds is therefore intentionally discarded. \code{translate_x} and
\code{translate_y} then provide explicit display offsets relative to the width
and height of the first/reference cloud. For example, \code{translate_x = 1.1}
places a second similarly sized eye to the right of the first with a gap of
approximately 10 percent of the reference width.

With \code{projection = "2D"}, \code{add = TRUE} redraws the CV3D-managed layers so
that the plotting limits expand automatically when newly added clouds fall
outside the previous range. Because base graphics are redrawn, unrelated
annotations added manually between calls are not retained by this redraw.

With \code{projection = "3D"}, the \code{rgl} package is required only at run time and
can therefore remain in \code{Suggests}. Added clouds use the same face-on camera
and the same translation convention as the 2D projection.

CV3D assumes spatial coordinates are supplied in micrometres (µm).
}
\examples{
data(cv3d_example_surface)

# Standardised base-R face-on projection; rgl is not required.
eye_view <- view_eye_face_on(
  cv3d_example_surface,
  projection = "2D"
)

# Add a second centred copy 1.1 reference-eye widths to the right.
view_eye_face_on(
  cv3d_example_surface,
  projection = "2D",
  add = TRUE,
  translate_x = 1.1,
  col = "grey50"
)

if (interactive() && requireNamespace("rgl", quietly = TRUE)) {
  view_eye_face_on(
    cv3d_example_surface,
    projection = "3D"
  )

  view_eye_face_on(
    cv3d_example_surface,
    projection = "3D",
    add = TRUE,
    translate_x = 1.1,
    col = "grey50"
  )
}
}
\seealso{
\code{\link[=align_pointcloud]{align_pointcloud()}}
}
