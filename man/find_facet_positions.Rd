% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/worfklow.R
\name{find_facet_positions}
\alias{find_facet_positions}
\title{Find facet poisition candidates}
\usage{
find_facet_positions(
  df = NULL,
  local_heights = NULL,
  local_height_threshold = 2.5,
  height_column = "local_height_log",
  neighbour_radius,
  merge_radius = neighbour_radius/2,
  weight_exponent = 1,
  max_iterations = 10,
  step_size = 1,
  tolerance = neighbour_radius * 0.001,
  min_cluster_size = 1,
  select_point = c("nearest_mode", "max_height"),
  return_details = FALSE,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{df}{Optional pre-thresholded input table. If supplied and it already
contains \code{x}, \code{y}, \code{z}, and \code{height_value}, it is used directly.}

\item{local_heights}{Local-height table used when \code{df} does not already
contain thresholded points with a \code{height_value} column.}

\item{local_height_threshold}{Numeric threshold applied to \code{height_column}.}

\item{height_column}{Character string naming the local-height column in
\code{local_heights}.}

\item{neighbour_radius}{Local neighbourhood radius used during condensation.}

\item{merge_radius}{Radius used to merge converged points into final
candidate groups. Defaults to \code{neighbour_radius / 2}.}

\item{weight_exponent}{Exponent applied to \code{height_value} during weighting.}

\item{max_iterations}{Maximum number of condensation iterations.}

\item{step_size}{Fraction of each shift toward the local weighted centre.}

\item{tolerance}{Early-stop tolerance for the maximum per-iteration shift.}

\item{min_cluster_size}{Minimum cluster size retained as a candidate.}

\item{select_point}{How to choose the final candidate point within a
converged group. Passed to \code{find_facet_candidates_condensed()}.}

\item{return_details}{Logical. If \code{TRUE}, return the full details list from
\code{find_facet_candidates_condensed()}. Otherwise return only the candidates
table.}

\item{verbose}{Logical. If \code{TRUE}, print short progress information.}

\item{...}{Currently unused. Kept so old project scripts can be adapted more
gently without failing on extra arguments immediately.}
}
\value{
Returns a tibble with the aligned coordinates in columns defined by
\verb{x_col, y_col, z_col}.

Data frame of facet candidates, or the full details list if
\code{return_details = TRUE}.
}
\description{
XYZ: create description and param defs
}
\examples{
xxx: add example and change above descsriptionand parameters

Find facet positions by thresholding and local condensation

This workflow helper first thresholds the local-height table and then runs
local mode condensation on the surviving points to obtain one facet
candidate per condensed point group.

}
