% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neighbours_edge_aware.R
\name{find_neighbours_edge_aware}
\alias{find_neighbours_edge_aware}
\title{Find Facet Neighbours with Edge-Aware Local Geometry}
\usage{
find_neighbours_edge_aware(
  df,
  edge_gap_threshold_deg = 90,
  x = "x",
  y = "y",
  z = "z",
  id = "ID",
  k = 6,
  core_k = 3,
  tangent_k = 12,
  knn_search = 20,
  interior_link_factor = 1.5,
  edge_link_factor = 1.4,
  shadow_angle_fraction = 2/3,
  shadow_angle_min_deg = 30,
  shadow_angle_max_deg = 45,
  shadow_radial_ratio = 1.15,
  shadow_min_remaining = 2,
  verbose = FALSE
)
}
\arguments{
\item{df}{Data frame or tibble containing facet IDs and x/y/z coordinates.}

\item{edge_gap_threshold_deg}{Numeric edge-detection threshold in degrees.
Default: \code{90}.}

\item{x, y, z, id}{Character. Column names for coordinates and facet IDs.}

\item{k}{Positive integer. Maximum local nearest-neighbour candidate count.
Default: \code{6}.}

\item{core_k}{Positive integer. Number of closest facet-centre distances used
for the robust local core spacing. Default: \code{3}.}

\item{tangent_k}{Positive integer. Number of nearby facets used to estimate
the local tangent plane. Default: \code{12}.}

\item{knn_search}{Positive integer. Number of nearest neighbours retained for
local calculations. Default: \code{20}.}

\item{interior_link_factor}{Positive numeric. Maximum link length relative to
local core spacing when both endpoints are interior. Default: \code{1.5}.}

\item{edge_link_factor}{Positive numeric. Maximum link length relative to
local core spacing when either endpoint is an edge facet. Default: \code{1.4}.}

\item{shadow_angle_fraction}{Positive numeric. Fraction of the median angular
spacing in six-neighbour interior facets used for the edge shadow-angle
cutoff. Default: \code{2/3}.}

\item{shadow_angle_min_deg, shadow_angle_max_deg}{Numeric bounds for the
derived shadow-angle cutoff. Defaults: \code{30} and \code{45} degrees.}

\item{shadow_radial_ratio}{Numeric. The farther of two near-collinear edge
candidates must be at least this many times farther away before it can be
removed. Default: \code{1.15}.}

\item{shadow_min_remaining}{Non-negative integer. Never prune an edge facet
below this number of retained neighbours. Default: \code{2}.}

\item{verbose}{Logical. Print a short method summary. Default: \code{FALSE}.}
}
\value{
The input data with additional columns \code{neighbours},
\code{number_of_neighbours}, \code{is_edge_facet}, \code{edge_angular_gap_deg},
\code{edge_gap_threshold_deg}, \code{neighbour_core_spacing_um}, and
\code{shadow_links_removed}. The returned object has attributes
\code{shadow_angle_threshold_deg}, \code{shadow_removed_links}, and
\code{neighbour_method}.
}
\description{
Finds reciprocal first-ring facet neighbours while treating the outer eye
boundary explicitly. The method uses reciprocal six-nearest-neighbour
membership, a robust local core-spacing gate, edge detection from a large
empty angular sector in the local tangent plane, and conservative
angle-shadow pruning for already detected edge facets.
}
\details{
The method is designed for an approximately first-ring compound-eye lattice
in which interior facets may have up to \code{k} direct neighbours but facets at
the sampled eye boundary should not be filled artificially with second-ring
neighbours. It proceeds in the following stages:
\enumerate{
\item Local tangent geometry and the largest angular gap are calculated as in
\code{\link[=detect_facet_edges]{detect_facet_edges()}}. Facets with a gap strictly greater than
\code{edge_gap_threshold_deg} are flagged as edge facets.
\item Candidate links are restricted to reciprocal \code{k}-nearest-neighbour
membership: facet A must be among facet B's first \code{k} candidates and vice
versa. This prevents one-sided long links.
\item A robust local core spacing is calculated for every facet as the median
distance to its \code{core_k} closest facet centres. Reciprocal links are kept
only if their 3D length is no greater than \code{interior_link_factor} times
the larger endpoint core spacing for an interior-interior link, or
\code{edge_link_factor} times that spacing when either endpoint is an edge.
\item CV3D derives an angular-shadow cutoff from the typical angular separation
of retained neighbours at six-neighbour interior facets. The median
reference separation is multiplied by \code{shadow_angle_fraction} and then
constrained to \code{shadow_angle_min_deg}--\code{shadow_angle_max_deg}.
\item Only at already detected edge facets, two retained neighbours that point
in nearly the same local tangent-plane direction are treated as a
possible first-ring/second-ring conflict. The farther candidate is pruned
only when it is at least \code{shadow_radial_ratio} times farther away. Links
are removed reciprocally, and pruning never reduces the focal facet below
\code{shadow_min_remaining} retained neighbours.
}

Edge facets are not forced to a predetermined neighbour count and there is
no refill stage. The resulting degree is therefore allowed to reflect the
available geometry naturally, with \code{k} acting as a maximum. The stored
\code{number_of_neighbours} column is intended both for downstream calculations
and for QC of the boundary solution.

The \code{neighbours} column contains semicolon-separated facet IDs. The
\code{neighbour_core_spacing_um} column is in µm. Attributes on the returned data
frame record the derived shadow cutoff, its interior reference statistics,
the individual removed shadow links, and the method identifier so that the
exact neighbour construction can be traced downstream.
}
\examples{
data(cv3d_example_facets)
nb <- find_neighbours_edge_aware(
  cv3d_example_facets,
  edge_gap_threshold_deg = 90
)
head(nb[, c("ID", "neighbours", "number_of_neighbours", "is_edge_facet")])

}
