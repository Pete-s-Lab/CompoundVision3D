% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculations.R
\name{ray_sphere_intersection}
\alias{ray_sphere_intersection}
\title{Calculate the Intersection of a 3D Ray and Sphere}
\usage{
ray_sphere_intersection(point, direction, sphere_center, sphere_radius)
}
\arguments{
\item{point}{Numeric vector of length three containing the x, y, and z
coordinates of the ray origin.}

\item{direction}{Numeric vector of length three giving the x, y, and z
components of the ray direction. The vector does not need to have unit
length.}

\item{sphere_center}{Numeric vector of length three containing the x, y, and
z coordinates of the sphere centre.}

\item{sphere_radius}{Numeric. Radius of the sphere. Must be greater than zero
and use the same spatial units as \code{point} and \code{sphere_center}.}
}
\value{
A numeric vector of length three containing the x, y, and z
coordinates of the nearest sphere intersection in the forward ray
direction. If no forward intersection exists, all three values are
\code{NA_real_}.
}
\description{
Calculates the nearest forward intersection between a ray in
three-dimensional space and a sphere. The ray originates at \code{point} and
extends in \code{direction}.
}
\details{
In CV3D, this function is used to project facet surface normals from facet
centres onto a corneal-projection sphere.
}
\examples{
ray_sphere_intersection(
  point = c(0, 0, 0),
  direction = c(1, 0, 0),
  sphere_center = c(0, 0, 0),
  sphere_radius = 10
)
}
