% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculations.R
\name{cartesian_to_view_angles}
\alias{cartesian_to_view_angles}
\title{Convert Cartesian Coordinates to View Angles}
\usage{
cartesian_to_view_angles(x, y, z, center = c(0, 0, 0), front_zero = TRUE)
}
\arguments{
\item{x}{Numeric vector of x coordinates.}

\item{y}{Numeric vector of y coordinates.}

\item{z}{Numeric vector of z coordinates.}

\item{center}{Numeric vector of length three specifying the x, y, and z
coordinates of the angular reference centre. Default: \code{c(0, 0, 0)}.}

\item{front_zero}{Logical. If \code{TRUE}, rotate azimuth by 90 degrees so that
the negative y-axis, corresponding to the anterior direction in the CV3D
global coordinate system, has azimuth 0 degrees. If \code{FALSE}, use the
conventional Cartesian azimuth measured from the positive x-axis.
Default: \code{TRUE}.}
}
\value{
A tibble with the columns \code{elevation} and \code{azimuth}, both in degrees.
Azimuth is wrapped to the interval from -180 degrees inclusive to +180
degrees exclusive. Coordinates located exactly at \code{center} return \code{NA}
because their angular direction is undefined.
}
\description{
Converts three-dimensional Cartesian coordinates to elevation and azimuth
relative to a specified centre.
}
\details{
Coordinates are first translated so that \code{center} becomes the origin.
Elevation is measured from the x-y plane, with positive values towards the
positive z-axis. By default, azimuth follows the CV3D anatomical convention
in which the anterior viewing direction (negative y-axis after landmark
alignment) is 0 degrees, the positive x-axis is +90 degrees, and the
negative x-axis is -90 degrees.
}
\examples{
directions <- cartesian_to_view_angles(
  x = c(0, 1, 0, -1),
  y = c(-1, 0, 1, 0),
  z = c(0, 0, 0, 0)
)
directions
}
